import { useState } from 'react';
import { Card, } from 'antd';

export default function AssetOperating() {

    return (
            <Card title="操作审计">
              操作审计
            </Card>
    )
}